name: Ephemeral Woodpecker macOS Agent

on:
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: macos-latest

    steps:
      # BƯỚC MỚI ĐÃ SỬA: Bật Screen Sharing VNC chuẩn cho macOS ARM64
      - name: Enable VNC Remote Desktop
        run: |
          # 1. Bật tính năng Screen Sharing / Remote Management
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate -configure -access -on -restart -agent -privs -all
          
          # 2. Đổi mật khẩu user runner thành "secret123" bằng sysadminctl (không đòi pass cũ)
          sudo sysadminctl -resetPasswordFor runner -newPassword secret123
          
          # 3. Đặt mật khẩu VNC trực tiếp là "secret123"
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -configure -client_settings -setvnclegacy -vnclegacy yes -client_settings -setvncpw -vnc-password secret123

      # Mở đường truyền kết nối SSH / Port Forwarding
      - name: Setup tmate Session
        uses: mxschmitt/action-tmate@v3
